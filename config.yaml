system:
  execution_mode: ml
  fram_schd: keras

ml_model:
  name: main_model

checkpoint:
  method: time
  interval: 300
  max_session_time: 900
  safety_buffer_seconds: 15
  keep_last: 3
  save_dir: checkpoints
